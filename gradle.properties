# Use the JDK bundled with the WPILib 2026 install (same JDK the robot code builds with).
# Portability note: override this in ~/.gradle/gradle.properties or via -Dorg.gradle.java.home
# on machines where WPILib lives elsewhere.
org.gradle.java.home=/Users/yajatparmar/wpilib/2026/jdk
org.gradle.jvmargs=-Xmx1g
org.gradle.caching=true

wpilibVersion=2026.2.1
