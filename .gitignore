.gradle/
.gradle-home*/
build/
*.class

# IDE
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# OS
.DS_Store

# Local data / logs pulled from robots (not source)
*.wpilog
*.revlog
/data/
/logs/
local.properties

# Read-only reference clones of the team robot repos (never committed into this repo)
/reference/

# Design-review screenshot scratch (regenerated by the impeccable skill)
/.impeccable-review/
/.impeccable/

# Local documentation checkouts + built knowledge index (data, not source)
/.knowledge/
*.kdb

# Closed-loop iteration state (per-iteration logs, journal, baseline) — local, not source
.loop/

# Smoke-test output from the CI MCP check, when run locally
tools.json

# Throwaway HOME used by the CI editor-installer check
.ci-home/
